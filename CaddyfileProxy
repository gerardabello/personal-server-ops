gaspt.gerard.sh {
    gzip
    proxy / gaspt:80
}

counters.gerard.sh {
    gzip
    proxy / counters-frontend:80
}

api.counters.gerard.sh {
    gzip
    proxy / counters-backend:3000
}

gerard.sh {
    gzip
    proxy / personal-site:80
}

api.unwel.ch {
    proxy / unwelch-backend:3000
}

unwel.ch {
    gzip
    proxy / unwelch-frontend:80
}

